apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: stayflexi
  labels:
    app: auth-service
spec:
  type: ClusterIP
  selector:
    app: auth-service
  ports:
  - name: http
    protocol: TCP
    port: 3001
    targetPort: http
