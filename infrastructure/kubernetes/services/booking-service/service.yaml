apiVersion: v1
kind: Service
metadata:
  name: booking-service
  namespace: stayflexi
  labels:
    app: booking-service
spec:
  type: ClusterIP
  selector:
    app: booking-service
  ports:
  - name: http
    protocol: TCP
    port: 3005
    targetPort: http
