apiVersion: v1
kind: Service
metadata:
  name: hotel-service
  namespace: stayflexi
  labels:
    app: hotel-service
spec:
  type: ClusterIP
  selector:
    app: hotel-service
  ports:
  - name: http
    protocol: TCP
    port: 3003
    targetPort: http
