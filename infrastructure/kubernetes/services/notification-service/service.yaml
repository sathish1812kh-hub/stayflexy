apiVersion: v1
kind: Service
metadata:
  name: notification-service
  namespace: stayflexi
  labels:
    app: notification-service
spec:
  type: ClusterIP
  selector:
    app: notification-service
  ports:
  - name: http
    protocol: TCP
    port: 3009
    targetPort: http
