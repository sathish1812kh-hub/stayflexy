apiVersion: v1
kind: Service
metadata:
  name: payment-service
  namespace: stayflexi
  labels:
    app: payment-service
spec:
  type: ClusterIP
  selector:
    app: payment-service
  ports:
  - name: http
    protocol: TCP
    port: 3006
    targetPort: http
