apiVersion: v1
kind: Service
metadata:
  name: pricing-engine-service
  namespace: stayflexi
  labels:
    app: pricing-engine-service
spec:
  type: ClusterIP
  selector:
    app: pricing-engine-service
  ports:
  - name: http
    protocol: TCP
    port: 3011
    targetPort: http
