apiVersion: v1
kind: Service
metadata:
  name: workflow-service
  namespace: stayflexi
  labels:
    app: workflow-service
spec:
  type: ClusterIP
  selector:
    app: workflow-service
  ports:
  - name: http
    protocol: TCP
    port: 3010
    targetPort: http
