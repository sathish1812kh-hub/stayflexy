apiVersion: v1
kind: Service
metadata:
  name: revenue-management-service
  namespace: stayflexi
  labels:
    app: revenue-management-service
spec:
  type: ClusterIP
  selector:
    app: revenue-management-service
  ports:
    - name: http
      protocol: TCP
      port: 3012
      targetPort: http
